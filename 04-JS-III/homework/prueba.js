function prueba(palabras){
    var palabras = ['Hello', 'World'];
    const frase;
  for (var i = 0; i < palabras.length; i++){
    frase = palabras[i] + ' ';
  }
  console.log(frase); 
}
prueba(palabras);